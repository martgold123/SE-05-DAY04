<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="viet.oop.seday04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEDAY04">
        <activity android:name=".DangNhapActivity"></activity>
        <activity android:name=".ChonTKB" />
        <activity android:name=".DangNhap" />
        <activity android:name=".XOATKB" />
        <activity android:name=".SuaTKB" />
        <activity android:name=".NhapTKB" />
        <activity android:name=".XemTKBSV" />
        <activity android:name=".XemTKB" />
        <activity android:name=".XemTKBGV" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>